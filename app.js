function asylumDemon() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Demônio do Asilo") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


function bellGargo() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Gárgolas do Sino") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


function capraDe() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Capra Demon") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


function ceaseLess() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Ceaseless Discharge") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


function cenTi() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Demônio Centopéia") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


function queeLag() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Bruxa do Caos, Queelag") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


function prisCilla() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Priscilla, A Mestiça") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


function gwynDolin() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Sol Negro Gwyndolin") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function fireSage() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Demon Firesage") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


function foUr() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Four Kings") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


function gaPing() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Gaping Dragon") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function sIf() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Grande Lobo Cinzento Sif") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function gwYn() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Gwyn, Lorde das Cinzas") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function ironGolem() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Golem de Ferro") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function moOn() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Borboleta do Luar") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function niTo() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Nito") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function oRn() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Ornstein E Smough") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function pIn() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Pinwheel") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function seAth() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Seath, o Sem Escamas") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function stRay() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Demônio Perdido") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function taUrus() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Taurus Demon") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function bEd() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "A Cama do Caos") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


function artoRias() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Artorias, o Andarilho do Abismo") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


function kalaMeet() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Dragão Negro Kalameet") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


function guarDian() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Guardião do Santuário") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};

function maNus() {
    // Obtém a seção HTML onde os resultados serão exibidos
    let section = document.getElementById("resultados-pesquisa");

    // Inicializa uma string vazia para armazenar os resultados
    let resultados = "";
    let titulo = "";
    let descricao = "";

    for (let dado of dados) {
        if (dado.titulo === "Manus, Pai do Abismo") {
            //cria um novo elemento
            resultados += `
        <div class="item-resultado">
        <img src=${dado.imagem} alt="imagem-jogo">
        <div>
            <h3>
                <a>${dado.titulo}</a>
            </h3>
            <p class="descricao-meta">${dado.descricao}</p>
            </div>
        </div>
        `;
        }
    };

    // Atribui os resultados gerados à seção HTML
    section.innerHTML = resultados;

};


